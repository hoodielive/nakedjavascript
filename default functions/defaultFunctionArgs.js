function makeAjaxRequest(url, method='GET') { 
    return method; 
    // logic to make request (Get Request) 
    // if (!method) {
    //  method = 'GET'; 
} 

makeAjaxRequest('google.com', undefined); 
makeAjaxRequest('google.com, 'POST'); 
