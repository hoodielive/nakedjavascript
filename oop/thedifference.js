var Anewobj = { 
    baseSalary: 40000, 
    overTime: 10, 
    rate: 20, 
    getWage: function(radius) { 
        return baseSalary * (overTime * rate)
    } 
} 

objis = new Anewobj


function Anewobj(radius) {
    return { 
        radius, 
        baseSalary: 40000, 
        overTime: 10, 
        rate: 20, 
        getWage: function(radius) { 
            return baseSalary * (overTime * rate)
        } 
} 

Encapsulation
