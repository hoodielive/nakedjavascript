#!/usr/bin/env node 

/** say what you want 
 *
 * but in Javascript we do NOT have classes 
 * what we do have is objects 
 **/

// every object has a function constructor that was used to create the object 
let x = {}; 
console.log(x.toString); 
